{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "otter run",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/otter/cli.py",
            "args": ["run", "--env", ".env.local"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
